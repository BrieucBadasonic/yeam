<div class="container mt-5">
  <h1 class="text-center">Liste de tous les binomes</h1>
  <%= form_tag binomes_path, method: :get do %>
    <%= label_tag 'Request Status' %>
    <%= select_tag "query", options_for_select(["A SUIVRE", "RAS", "CONTACT PERDU", "DOSSIER CLOTURE"]) %>
    <%= submit_tag "Search", class: "btn btn-warning" %>
  <% end %>

  <% if @binomes %>
    <% @binomes.each do |binome| %>
      <div class="binome_dasboard_card">
        <div><strong>Benevole:</strong> <%= binome.benevole.first_name %> <%= binome.benevole.last_name %></div>
        <div><strong>Client:</strong> <%= binome.client.first_name %> <%= binome.client.last_name %></div>
        <div><strong>Request Status:</strong> <%= binome.request_status %></div>
        <div><strong>Date de mise en relation:</strong> <%= binome.connection_date %></div>
      </div>
    <% end %>
  <% end %>
</div>
